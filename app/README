Notes on compiling the new TBC application
=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-

The test environment for this build is Ubuntu 16.04 LTS
The new TBC application requires Qt version 5 in order to compile.

To install the required Qt 5 environment on Ubuntu 16.04 LTS simply issue the following command:

sudo apt-get install qt5-default

This command will install the required Qt 5 libraries from the Ubuntu repository and make Qt5 the default for all compilation
